export const SERVER_INSTRUCTIONS = `This server provides a local knowledge graph, scope lock, reasoning log, and verification discipline for this codebase. Mandatory workflow: (1) call get_project_overview and init_or_refresh_graph at session start, (2) call query_best_practices for any relevant domain before writing new code, (3) call declare_task_scope before making any edit, (4) call check_scope before every file write and stop to ask the user if the result is needs_approval or hard_stop, (5) call record_change immediately after every file create/modify/delete, (6) before the final answer, run the actual build/test/start command needed to verify the work. If a dependency or command is missing, fix it when safely in scope or clearly report the blocker. Do not tell the user to run an untested command as if it were verified.

All data is local to the project. The graph stores signatures, docstrings, line ranges, tags, and hashes only; it never stores full source bodies.`;
